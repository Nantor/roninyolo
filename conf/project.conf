# Use a different image for this project
RY_IMAGE="myregistry.local/opencode-projectX:latest"

# Additional volumes only for this project
RY_ADDITIONAL_VOLUMES+=(
  "$PWD/.cache:/work/.cache"
)

# Example: override user just for this project (optional)
# RY_DOCKER_USER="1001:1001"
